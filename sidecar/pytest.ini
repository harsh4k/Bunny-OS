[pytest]
# Prefer `python -m unittest discover -s sidecar/tests` (verify-beta).
# This config makes naive `pytest` from repo root or sidecar/ find the fakes package.
pythonpath = tests
testpaths = tests
